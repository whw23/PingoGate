# core-rs/storage/Cargo.toml
[package]
name = "pingogate-storage"
description = "Storage abstraction: snapshot source, KeyVault, and secret reference resolution."
version.workspace = true
edition.workspace = true

[dependencies]
pingogate-core-types = { path = "../core" }
pingogate-snapshot = { path = "../snapshot" }
pingogate-keyvault = { path = "../keyvault" }
thiserror = { workspace = true }
