# Canonical node order — the byte-parity baseline. A recipe's
# nodes: [...] replaces this list.
order:
  - size-budget
  - gather-context
  - plan-doc
  - constitution-check
  - side-files
  - handoff
